apiVersion: v1
kind: Service
metadata:
  name: {{ include "jag-bot.fullname" . }}
  labels:
    {{- include "jag-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jag-bot.selectorLabels" . | nindent 4 }}
